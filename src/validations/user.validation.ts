import Joi from "joi"
import { JoiObject } from "../types/router";

// export const createUser: JoiObject = {
//     body: Joi.object({
//         email: Joi.string().email().required(),
//         password: Joi.string().min(8).max(20).alphanum().required()
//     }),
// };