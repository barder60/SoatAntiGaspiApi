import { Request, Response } from "express";
import * as offerServices from "../services/offer.services";

// export async function getAlloffers(req: Request, res: Response) {
//     res.json(await offerServices.findAlloffers());
// }
